# Module 3 — ArgoCD: the guestbook Service (vendored into your gitops repo).
# Copy alongside guestbook-ui-deployment.yaml into apps/guestbook/.
apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: guestbook-ui
